#Übung 
#EPR
#Medieninformatik  

---
![[_Medieninformatik/Vorherige Semester/1. Semester/EPR/Übung/Anhänge/15_Rekursive Datenstrukturen.pdf]]

# Aufgabe 1
Die Methode verursacht, dass die Liste verkürzt wird, indem der Listenkopf durch den Nachfolger ersetzt wird. Am Ende wird das letzte Element der Liste ausgegeben.

Der Code sollte folgendermaßen geändert werden:
```java

public String m() {
	String s;
	Listenknoten letztesElement = this.listenkopf;
	if (letztesElement == null) {
		s = null;
	} else {
		while (letztesElement.gibNachfolger() != null) {
			letztesElement = letztesElement.gibNachfolger();
		}
			s = letztesElement.gibInhalt();
	} 
}
```

# Aufgabe 2
```java
public int gibLaenge(){

	int listeLaenge;
	Listenknoten vorgaenger = this.listenkopf;
	if(vorgaenger == null) {
		listenLaenge = 0;
	} else {
		listenLaenge = 1;
		while (nachfolger.gibNachfolger() != null) {
			listenLaenge += 1;
		}	
	}
	return listeLaenge;
}
```

# Aufgabe 3
```java
public boolean enthaelt(String zeichen){

	Listenknoten vorgaenger = this.listenkopf;
	boolean enthalten;
	
	if(vorgaenger == null){
		enthalten = false;
	} else {
	while (vorgaenger.gibNachfolger() != null || enthalten == false){
		enthalten = vorgaenger.gibInhalt() == zeichen;
		vorgaenger = vorgaenger.gibNachfolger;
	}
	return enthalten;

	
}
```

# Aufgabe 4
```java
public void fuegeHinzu(String s, String v) {

	Listenknoten vorgaenger = this.listenkopf;

	if(v != null){
		while(vorgaenger.gibNachfolger() != null && !v.equals(vorgaenger.gibInhalt())) { 
		vorgaenger = vorgeanger.gibNachfolger();
		
		}
		Listenknoten knoten = new Listenknoten(s, k.(gibNachfolger()));
		k.setzeNachfolger(knoten);
	}

}
```

# Aufgabe 5
```java
// in Binaerbaum
public int gibAnzahl(){
	return anzahl = wurzel == null
		? 0
		: wurzel.gibAnzahl();

}

// in Baumknoten
	public int gibAnzahl(){
	
	return 1
			+ (linkerTeilbaum == null ? 0 : linkerTeilbaum.gibAnzahl())
			+ (rechterTeilbaum == null ? 0 : rechterTeilbaum.gibAnzahl())
	
	}
```

# Aufgabe 6
```java
// in Binaerbaum
public boolean enthaelt(String k){

	return wurzel != null && wurzel.enthaelt(k); 

}

// in Baumknoten
public boolean enthaelt(String k){

	return k.equals(this.inhalt) 
	|| linkerTeilbaum != null && linkerTeilbaum.enthealt(k) 
	|| rechterTeilbaum != null rechterTeilbaum.enthaelt(k);

}
```

