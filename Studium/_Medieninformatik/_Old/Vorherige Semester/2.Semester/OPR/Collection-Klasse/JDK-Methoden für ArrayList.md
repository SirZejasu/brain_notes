#Themen 
#OPR
#Medieninformatik 

---
# Quellen
s. Skript

# Typische Methoden für ArrayList

| Methoden                        | Beschreibung (Alternativ)                                                |
| ------------------------------- | ------------------------------------------------------------------------ |
| boolean add (E o)               | Fügt einen spezifischen Element am Ende der Liste.                       |
| void add (int index, E element) | E ist der Typ eines Objekts innerhalb einer Liste.                       |
| E get(int index)                | Ruft das Element in aus der Position i                                   |
| E remove(int index)             | Nach dem entfernen des Elements wird das entfernte Objekt zurückgegeben. |
| boolean remove(Object o)        | Entfernt das erste Objekt, dass der im Parameter gleich ist.             |
| int size()                      | Ruft die Anzahl der Elemente ab.                                         |
| boolean contains(Object o)      |             Sagt aus, ob die Liste das Element enthält (True, wenn ja).                                                             |
| boolean isEmpty()               | Ist wahr, wenn die Liste leer ist.                                                               |
| Object[] toArray()              |        Erstellt einen Array aus einer Liste mit Elementen des Typs Object.                                                                  |

> [!example] Beispiel
> ![[Pasted image 20230420173051.png]]



